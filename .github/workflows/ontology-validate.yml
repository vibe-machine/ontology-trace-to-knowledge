name: ontology-validate

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    uses: vibe-machine/ontology-tooling/.github/workflows/ontology-release.yml@main
    with:
      mode: validate-only

